---

interface Props {
	text: string;
    url: string;
}

const { text, url } = Astro.props;
---

<a href={url} class="p-4 text-white hover:bg-[#f9c36731] rounded-2xl transition-all flex items-center justify-center gap-4">{text} <slot/> </a>